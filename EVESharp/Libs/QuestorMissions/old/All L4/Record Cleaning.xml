<?xml version="1.0" encoding="utf-8"?>
<mission>
  <pockets>
    <pocket id="0">
       
      <actions>
	    <action name="MoveToBackground"> <!-- initiate movement to to the "target" and move on to the next action, using this action assumes that the actions used after this will not move the ship as we likely need to stay moving toward the target-->
		  <parameter name="Target" value="Gate to Patrol Area" />
          <parameter name="Distance" value="2000"/>
		</action>
        <action name="WaitUntilTargeted">
          <parameter name="Timeout" value="15" />
        </action>
        <action name="ClearPocket">
          <parameter name="Timeout" value="15" />
        </action>
        <action name="Activate">
          <parameter name="Target" value="Gate to Patrol Area" />
        </action>
      </actions>
    </pocket>
    <pocket id="1">
      <actions>
		<action name="MoveToBackground"> <!-- initiate movement to to the "target" and move on to the next action, using this action assumes that the actions used after this will not move the ship as we likely need to stay moving toward the target-->
		  <parameter name="Target" value="Gate to Pirate Base" />
          <parameter name="Distance" value="2000"/>
		</action>
        <action name="WaitUntilTargeted">
          <parameter name="Timeout" value="15" />
        </action>
        <action name="ClearPocket">
          <parameter name="Timeout" value="15" />
        </action>
        <action name="Activate">
          <parameter name="Target" value="Gate to Pirate Base" />
        </action>
      </actions>
    </pocket>
    <pocket id="2">
      <actions>
		<action name="MoveToBackground"> <!-- initiate movement to to the "target" and move on to the next action, using this action assumes that the actions used after this will not move the ship as we likely need to stay moving toward the target-->
		  <parameter name="Target" value="Asteroid Pirate Station" />
          <parameter name="Distance" value="2000"/>
		</action>
        <action name="WaitUntilTargeted">
          <parameter name="Timeout" value="15" />
        </action>
        <action name="ClearPocket">
          <parameter name="Timeout" value="15" />
        </action>
		<action name="LootItem">
          <parameter name="Item" value="Hostages" />
          <parameter name="Target" value="Asteroid Pirate Station" />
        </action>
        <action name="Done" />
      </actions>
    </pocket>
  </pockets>
</mission>